# Agent 编排资料

LangGraph 流转图与节点说明存放于此。`langgraph-flow.md` 已填入用户提供的去除测算版流转图，可供 T-012 开发引用。

| 文件 / 目录 | 用途 |
|-------------|------|
| `langgraph-flow.md` | **主流转图**（已填入去除测算版；Mermaid + 节点/分支/Trace 映射） |
| `tool-richness-roadmap.md` | **工具丰富度路线图**（T-020 问数 → T-021 估值 → …；验收后按此续作） |
| `nodes/` | 可选：各节点输入输出、Prompt 补充说明 |

规范详见 `docs/Plan.md`「二点六 · LangGraph Agent 流转图」。
